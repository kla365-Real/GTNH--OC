local Robot_Func = require("Robot_Func")
local rb = require("robot")
local component = require("component")
local geo = component.geolyzer
local Farm_Func = require("Farm_Func")
local sides = require("sides")
local Storage_Func = require("Storage_Func")
local rs = component.redstone
local sides = require("sides")
local Crossbreed = require("Crossbreed")
local navi = component.navigation
local inv = component.inventory_controller
slot = 0
--for x,t in pairs(Farm_Func.Analyze()) do
--    print(x,t)
--end
--for x,y in pairs(require("component").geolyzer.analyze((require("sides")).down)) do print(x,y) end
--local Data = Farm_Func.Analyze()
--for x,y in pairs(Data) do print(x,y) end
--for x,y in pairs(Storage_Func.StorageSize()) do
--    print(x,y)
--end
--Storage_Func.MoveCroptoDislocation()
--for x,y in pairs(geo.analyze(sides.down)) do
--    print(x,y)
--end
--print((geo.analyze(sides.down))["crop:name"])
--Crossbreed.PlotCrossbreed()
--rb.turnRight()
--print(Robot_Func.getDirection())
--Robot_Func.turntoDirection("south")
--rb.useDown()
print(rb.count(2))
rb.select(2)
inv.suckFromSlot(0,2,(64 - rb.count(2)))
